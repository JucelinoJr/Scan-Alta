import React from 'react';
import { View } from 'react-native';
import Camera from './Camera';

class RequestCamera extends React.Component{
    render(){
        return(
            <Camera/>
        );
    }
}

export default RequestCamera;